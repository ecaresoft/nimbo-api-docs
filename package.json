{
  "name": "nimbo-api-docs",
  "private": true,
  "version": "1.0.0",
  "scripts": { "dev": "mint dev", "validate": "ruby scripts/check-contracts.rb && mint validate", "test": "ruby test/sync_api_test.rb", "links": "mint broken-links" },
  "devDependencies": { "mint": "4.2.634" }
}
